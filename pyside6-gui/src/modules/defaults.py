"""Shared defaults for CLI args and UI controls."""

DEFAULTS = {
    "camera_id": 0,
    "resolution": None,   # None = auto-select highest
    "model": "reason2",
    "interval": 1000,
    "prompt": "Describe this image in one sentence without coordinates or numbers.",
    "max_tokens": 512,
}
