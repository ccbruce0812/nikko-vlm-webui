# moondream2 + Router + Live-VLM-WebUI
services:
  router:
    build: ../router
    container_name: router
    ports: ["8080:8080"]
    networks: [vlm-net]
    restart: unless-stopped

  moondream2:
    build: .
    image: moondream2
    container_name: moondream2
    runtime: nvidia
    volumes:
      - /home/brucehsu/project/models/moondream2:/model:ro
    networks: [vlm-net]
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  live-vlm-webui:
    build: ../live-vlm-webui
    container_name: live-vlm-webui
    network_mode: host
    privileged: true
    runtime: nvidia
    volumes:
      - /sys:/sys:ro
      - /run/jtop.sock:/run/jtop.sock:ro
    restart: unless-stopped

networks:
  vlm-net:
    driver: bridge
